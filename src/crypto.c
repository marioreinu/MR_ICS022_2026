#include "crypto.h"
#include <stdio.h>

int encrypt_file(const char *filename)
{
    printf("Encrypting: %s\n", filename);
    return 0;
}

int decrypt_file(const char *filename)
{
    printf("Decrypting: %s\n", filename);
    return 0;
}